export type PlayerImages = {
  down: HTMLImageElement | null;
  up: HTMLImageElement | null;
  left: HTMLImageElement | null;
  right: HTMLImageElement | null;
};

export type ColorName = "blue" | "red" | "yellow" | "green" | "pink" | "purple";
